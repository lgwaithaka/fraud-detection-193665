# ============================================================
# Motor Insurance Fraud Detector — DSA 8502 | 193665
# requirements.txt — RENDER.COM FREE TIER COMPATIBLE
# All packages pinned to versions with pre-built binary wheels
# for Python 3.11 + Linux x86_64. Zero source compilation.
# ============================================================

# ── Data ─────────────────────────────────────────────────────
# pandas 2.0.3 — last version with guaranteed cp311 manylinux wheel
# DO NOT upgrade to 2.1.x or 2.2.x — those require source build on Render
pandas==2.0.3
numpy==1.26.4
matplotlib==3.8.3
seaborn==0.13.2

# ── Machine Learning ─────────────────────────────────────────
scikit-learn==1.4.2
xgboost==2.0.3
imbalanced-learn==0.12.2
optuna==3.6.1
joblib==1.4.0

# ── Explainability ────────────────────────────────────────────
shap==0.44.1
lime==0.2.0.1

# ── API backend ───────────────────────────────────────────────
fastapi==0.110.0
uvicorn[standard]==0.29.0
pydantic==2.6.4
python-multipart==0.0.9

# ── Frontend ──────────────────────────────────────────────────
streamlit==1.33.0
httpx==0.27.0
